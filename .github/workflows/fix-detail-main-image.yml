name: Fix detail main image renderer

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/fix-detail-main-image.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Replace background renderer with a real rounded image
        run: |
          python3 - <<'PY'
          from pathlib import Path

          p = Path('src/AppExact.jsx')
          s = p.read_text()
          old = '''{imgs[i]?<div className="mxGalleryImage" role="img" aria-label={`${car.brand||''} ${car.model||''}`} style={{width:'100%',height:'100%',backgroundImage:`url(${imgs[i]})`,backgroundRepeat:'no-repeat',backgroundPosition:'center center',backgroundSize:'contain'}}/>:<CarFront/>}'''
          new = '''{imgs[i]?<div className="mxGalleryImage" style={{width:'calc(100% - 12px)',height:'calc(100% - 12px)',display:'flex',alignItems:'center',justifyContent:'center',overflow:'hidden',borderRadius:'18px',background:'#f5f6f7',margin:'auto'}}><img src={imgs[i]} alt={`${car.brand||''} ${car.model||''}`} style={{display:'block',width:'auto',height:'auto',maxWidth:'100%',maxHeight:'100%',objectFit:'contain',objectPosition:'center',borderRadius:'16px'}}/></div>:<CarFront/>}'''
          count = s.count(old)
          if count != 1:
              raise SystemExit(f'Expected exactly one background renderer, found {count}')
          p.write_text(s.replace(old, new, 1))
          PY
      - name: Commit gallery component fix
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/AppExact.jsx
          git commit -m "Render real rounded image in car detail gallery"
          git push
