const FALLBACK_IMAGE='https://res.cloudinary.com/bjlglhaw/image/upload/v1788117338/maurione-app-icon.png';
function clean(value='',max=500){return String(value||'').replace(/\s+/g,' ').trim().slice(0,max)}
function escapeHtml(value=''){return String(value).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;').replace(/'/g,'&#39;')}
function safeImage(value){const image=clean(value,1800);try{const url=new URL(image);return url.protocol==='https:'?url.toString():FALLBACK_IMAGE}catch{return FALLBACK_IMAGE}}
function numeric(value){const n=Number(clean(value).replace(/[^0-9.]/g,'')||0);return Number.isFinite(n)?n:0}
function buildShareUrl(origin,carId,meta){const p=new URLSearchParams();if(meta.name)p.set('n',meta.name);if(meta.price)p.set('p',meta.price);if(meta.image&&meta.image!==FALLBACK_IMAGE)p.set('i',meta.image);if(meta.mileage)p.set('m',meta.mileage);if(meta.fuel)p.set('f',meta.fuel);if(meta.transmission)p.set('tr',meta.transmission);if(meta.location)p.set('l',meta.location);const q=p.toString();return `${origin}/share/car/${encodeURIComponent(carId)}${q?`?${q}`:''}`}
function pageHtml({meta,canonicalUrl,shareUrl}){const title=meta.name?`${meta.name} | MauriOne`:'MauriOne Cars';const parts=[];if(meta.name)parts.push(meta.name);if(meta.price)parts.push(`Preço ${meta.price}`);if(meta.mileage)parts.push(`${meta.mileage} km`);if(meta.fuel)parts.push(meta.fuel);if(meta.transmission)parts.push(meta.transmission);if(meta.location)parts.push(meta.location);const description=parts.length?`${parts.join(' · ')} — Veja fotografias, especificações e contacto na MauriOne.`:'Veja os detalhes e fotografias do carro na MauriOne.';const price=numeric(meta.price),mileage=numeric(meta.mileage);const structured={'@context':'https://schema.org','@type':'Vehicle',name:meta.name||'Carro na MauriOne',url:canonicalUrl,image:[meta.image],...(mileage>0?{mileageFromOdometer:{'@type':'QuantitativeValue',value:mileage,unitCode:'KMT'}}:{}),...(meta.fuel?{fuelType:meta.fuel}:{}),...(meta.transmission?{vehicleTransmission:meta.transmission}:{}),...(price>0?{offers:{'@type':'Offer',priceCurrency:'MRU',price,availability:'https://schema.org/InStock',url:canonicalUrl}}:{})};const safeStructured=JSON.stringify(structured).replace(/</g,'\\u003c');return `<!doctype html><html lang="pt-AO" dir="ltr"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${escapeHtml(title)}</title><meta name="description" content="${escapeHtml(description)}"><meta name="robots" content="noindex,follow"><link rel="canonical" href="${escapeHtml(canonicalUrl)}"><meta property="og:site_name" content="MauriOne"><meta property="og:type" content="website"><meta property="og:locale" content="pt_AO"><meta property="og:title" content="${escapeHtml(title)}"><meta property="og:description" content="${escapeHtml(description)}"><meta property="og:image" content="${escapeHtml(meta.image)}"><meta property="og:image:alt" content="${escapeHtml(meta.name||'MauriOne')}"><meta property="og:url" content="${escapeHtml(shareUrl)}"><meta name="twitter:card" content="summary_large_image"><meta name="twitter:title" content="${escapeHtml(title)}"><meta name="twitter:description" content="${escapeHtml(description)}"><meta name="twitter:image" content="${escapeHtml(meta.image)}"><script type="application/ld+json">${safeStructured}</script><meta http-equiv="refresh" content="0;url=${escapeHtml(canonicalUrl)}"></head><body><p>A abrir o carro na MauriOne...</p><script>location.replace(${JSON.stringify(canonicalUrl)});</script><noscript><a href="${escapeHtml(canonicalUrl)}">Abrir anúncio</a></noscript></body></html>`}
export default function handler(req,res){const carId=clean(req.query?.id,180);if(!carId||!/^[A-Za-z0-9_-]+$/.test(carId)){res.status(400).send('Invalid car id');return}const forwardedHost=String(req.headers['x-forwarded-host']||req.headers.host||'maurione-cars.vercel.app').split(',')[0].trim(),forwardedProto=String(req.headers['x-forwarded-proto']||'https').split(',')[0].trim(),origin=`${forwardedProto}://${forwardedHost}`,canonicalUrl=`${origin}/cars/${encodeURIComponent(carId)}`;const meta={name:clean(req.query?.n,180),price:clean(req.query?.p,80),image:safeImage(req.query?.i),mileage:clean(req.query?.m,80),fuel:clean(req.query?.f,80),transmission:clean(req.query?.tr,80),location:clean(req.query?.l,120)},shareUrl=buildShareUrl(origin,carId,meta);res.setHeader('Content-Type','text/html; charset=utf-8');res.setHeader('Cache-Control','public, s-maxage=300, stale-while-revalidate=86400');res.setHeader('X-MauriOne-Share',meta.name&&meta.image!==FALLBACK_IMAGE?'enriched':'fallback');res.status(200).send(pageHtml({meta,canonicalUrl,shareUrl}))}
